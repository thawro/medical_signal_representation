# @package _global_

defaults:
  - override /datamodule: ${dataset}.yaml
  - override /model: ${model_name}_classifier.yaml
  - override /callbacks: default.yaml
  - override /trainer: dl_classifier_trainer.yaml
  - override /logger: wandb_logger.yaml
  - override /plotter: plotly.yaml

# model:
#   # TODO model params
